# Review Prompt: RAG Evaluation Comparison Filters

Review the comparison filters for these concerns:

- Filtering is frontend-only and does not introduce direct FastAPI calls.
- Empty filtered results show a clear state instead of blank rankings.
- Strategy and experiment option lists are derived from the loaded summary rows.
- The existing global summary metrics remain visible and unchanged.
- Layout stays responsive on narrow screens.
